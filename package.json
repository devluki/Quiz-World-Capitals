{
  "name": "quiz",
  "version": "1.0.0",
  "description": "World Capitals Quiz",
  "scripts": {
    "test": "vitest --run --reporter verbose",
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html ",
    "deploy": "npm run build && git add . &&  git push origin master"
  },
  "homepage": "https://devluki.github.io/Quiz-World-Capitals/",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "parcel": "^2.7.0",
    "sass": "^1.54.4",
    "vitest": "^0.24.5"
  }
}
